// goHome.js

// JavaScript code to navigate to the homepage
const goHomeButton = document.getElementById('goHome');

goHomeButton.addEventListener('click', function() {
    // Change the URL to the homepage
    window.location.href = 'index.html';
});